# Server-side secrets (Netlify Functions / Netlify env) — never sent to the browser
PRIVY_APP_ID=
PRIVY_APP_SECRET=
DEFINDEX_API_KEY=

# Public values (exposed to the Next.js frontend)
NEXT_PUBLIC_PRIVY_APP_ID=
NEXT_PUBLIC_STELLAR_NETWORK=testnet
